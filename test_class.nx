کلاس طالب_علم {
    طریقہ init(نام، نمبر) {
        خود.نام = نام;
        خود.نمبر = نمبر;
    }
    طریقہ دکھاؤ() {
        لکھو("طالب علم: " + خود.نام);
        لکھو("نمبر: " + خود.نمبر);
    }
}
متغیر s = نیا طالب_علم("علی", 95);
s.دکھاؤ();
