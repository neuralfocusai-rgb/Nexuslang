// NexusLang v11 - Listas (فہرست)
متغیر اعداد = فہرست(30, 10, 20);
لکھو(اعداد);

شامل(اعداد, 40);
لکھو("بعد شامل:");
لکھو(اعداد);

ترتیب(اعداد);
لکھو("مرتب:");
لکھو(اعداد);

لکھو("پہلا عنصر: " + عنصر(اعداد, 0));
لکھو("لمبائی: " + لمبائی(اعداد));

الٹو(اعداد);
لکھو("الٹا:");
لکھو(اعداد);
