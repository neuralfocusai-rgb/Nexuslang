// Clases en Urdu
کلاس طالب {
    طریقہ نیا(نام) {
        خود.نام = نام;
    }
    طریقہ سلام() {
        واپس "سلام " + خود.نام;
    }
}

متغیر t = طالب("علی");
لکھو(t.سلام());
لکھو(t.نام);
t.نام = "فاطمہ";
لکھو(t.نام);
